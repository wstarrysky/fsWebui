# 安全原则问题
- 文件写入 (Write, Edit)
- 文件删除/移动
- 执行shell命令 (如 git push, npm install 等)
- 创建新文件
- 如果过程中模型自己想要通过mcp执行写入操作也是拒绝, 读取操作允许
- 待继续补充场景
# rules规则热加载问题
- 只发给LLM, 不需要向用户展示
- 规则文件放在根目录吧
# 简化页面提示
- 工具调用日志不用展示
- 还有哪些会展示出来?给我一个清单, 我再进行挑选


| 元素         | 说明                                     | 默认状态 | 你要保留?  |
  |--------------|------------------------------------------|----------|--------|
| 消息正文     | Claude的回复内容（流式）                 | ✅ 显示  | ✅ 保留   |
| 用户消息     | 你发送的问题                             | ✅ 显示  | ✅ 保留   |
| 时间戳       | 每条消息的时间                           | ✅ 显示  | 保留     |
| 工具调用日志 | 🔧 Bash、Read等工具被调用时的记录        | ✅ 显示  | ❌ 隐藏   |
| 工具结果     | ✓ 工具执行的结果（代码预览、grep结果等） | ✅ 显示  | 隐藏     |
| 系统消息     | ⚙️ 初始化、错误等系统提示                | ✅ 显示  | 错误进行显示 |
| 思考过程     | 💭 Claude的推理过程                      | ✅ 显示  | 推理过程显示 |
| 待办事项     | 📋 TodoList更新                          | ✅ 显示  | 待办事项显示 |
| 计划消息     | 📋 Plan模式的计划内容                    | ✅ 显示  | 计划内容显示 |
| 加载动画     | "Thinking..." 旋转图标                   | ✅ 显示  | 加载动画显示 |
| 权限弹窗     | 工具权限请求对话框（你拒绝后就不需要了） | ✅ 显示  | 工具权限,如果是读相关的就显示, 写相关的不显示;


1. 可以接受给测试提供代码逻辑的文字描述, 业务逻辑, 流程图; 但是不可以接受把大段大段的源码暴露出去
2. 目的是先给出一个最小可行性Demo, 可以把未来要达到的安全级别与局域网内任意访问分为多个阶梯计划, 先按最简单的来
3. 也是和2一样, 分为多个阶梯计划, 先按最简单的来, 即敏捷开发
4. 也是和2一样, 分为多个阶梯计划, 先按最简单的来